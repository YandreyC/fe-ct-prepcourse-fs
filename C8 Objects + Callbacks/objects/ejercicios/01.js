const obtenerValorPropiedad = (objeto, propiedad) => {
   // Recibe un objeto y el nombre de una propiedad.
   // Devuelve el valor de esta propiedad.
   // Tu código:
   const numero = objeto[propiedad];

   return numero;
};

module.exports = obtenerValorPropiedad;
